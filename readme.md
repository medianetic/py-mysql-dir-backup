# Backup: Directory & MySQL Database

A simple python script which:

- make a mysqldump of a database and
- zips a defined directory

I use this on my own for moving a contao installation from one server to another.